export const showSuccess = (message: string) => console.log(message);
export const showError = (message: string) => console.error(message);
export const showToast = { copied: () => console.log("Link copied!") };
